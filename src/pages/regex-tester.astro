---
import ToolLayout from "../layouts/ToolLayout.astro";
import RegexTest from "../components/ToolComponent/RegexTest";
import regex from "../content/regex.json"

const {toolName, metaDescription, introTitle, introText, steps, faqs, glossary, initialText, initialDesc} = regex

---

<ToolLayout title={toolName} desc={metaDescription} introText={introText} introTitle={introTitle} steps={steps} faqs={faqs} glossary={glossary} initialText={initialText} initialDesc = {initialDesc}>
    <div class="flex flex-col p-3 gap-2">
        <div class="text-white text-2xl flex justify-between font-bold">
            Regex Tester
        </div>
        <div class="text-gray-500 flex justify-between">
            <p class="text-sm">Test and debug regular expressions</p>
        </div>

        <div class="flex justify-start">
           <RegexTest client:load/>
        </div>

        <div>
            <div>
                <p>
                    A Guide To Our JSON World
                </p>

            </div>
        </div>
    </div>
</ToolLayout>